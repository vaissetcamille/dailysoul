<?xml version="1.0" encoding="utf-8"?>
<!--
============================================================
Daily Soul Journal - Android Manifest
============================================================
Declares the app's package, permissions, activities, and
services. Minimal permissions policy: only what's truly needed.
- RECEIVE_BOOT_COMPLETED: to restart daily reminder alarms
- VIBRATE: for gentle notification vibration
- POST_NOTIFICATIONS: required on Android 13+ for push alerts
============================================================
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.dailysoul.journal">

    <!-- Minimal permissions for offline journaling + reminders -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".DailySoulApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/backup_rules"
        android:fullBackupContent="@xml/backup_content"
        android:icon="@drawable/ic_launcher"
        android:roundIcon="@drawable/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.DailySoul.Splash"
        tools:ignore="DeprecatedForBaseConfig">

        <!-- Main Journal Entry (Home) Screen -->
        <activity
            android:name=".ui.activity.MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Mood Selection Screen (optional pre-journaling step) -->
        <activity
            android:name=".ui.activity.MoodActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- Journal History List Screen -->
        <activity
            android:name=".ui.activity.HistoryActivity"
            android:exported="false" />

        <!-- Single Journal Entry Reader Screen -->
        <activity
            android:name=".ui.activity.ReadActivity"
            android:exported="false" />

        <!-- App Settings Screen -->
        <activity
            android:name=".ui.activity.SettingsActivity"
            android:exported="false" />

        <!-- Background receiver: restarts daily reminder alarm after reboot -->
        <receiver
            android:name=".util.BootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

        <!-- Background receiver: fires the actual notification at alarm time -->
        <receiver
            android:name=".util.AlarmReceiver"
            android:exported="false" />
    </application>
</manifest>
